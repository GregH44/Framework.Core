namespace Framework.Core.Enums
{
    internal static class GlobalEnums
    {
        internal enum Api
        {
            Add,
            Delete,
            Get,
            GetList,
            Update
        }

        internal enum SqlBuildOperation
        {
            Select,
            Delete
        }
    }
}
